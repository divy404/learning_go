package main

import (
	"fmt"
)

func main() {
	fmt.Println("i will do this for sure thissss timee letssss goooo")
	var money int = 9000
	fmt.Println(money)
	var floatcurrency = 123.23
	fmt.Println(floatcurrency)
	//  if we assign a data type to a variable we cant change it
	//  we can use just var it will detect automatically the data type
	person := 123
	fmt.Println(person)
	//  this is also a way to declare variables
	// ***********IMP********* -> if we assign a variable or func name with
	//  capital letter we can use it in any package. if small letter it remains in the same file. 


}
